#pragma once
#include <iostream>
#include "Person.h"
void Sort(Person*& person, Hp hp); // Function to sort the Person array by surname
void New(Person*& group, Hp& hp); // Function to add a new Person to the list
